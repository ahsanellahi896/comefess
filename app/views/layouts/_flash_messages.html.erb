<div class='container'>
  <% flash.each do |key, value| %>
    <div class="alert alert-<%= key == 'notice' ? 'success' : 'danger' %>">
      <a class='btn-link pull-right' href='#' data-dismiss='alert' class='close'>×</a>
        <ul>
          <li>
            <%= value %>
          </li>
        </ul>
    </div>
  <% end %>
</div>
